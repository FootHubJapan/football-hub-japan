# スマホUI/UX サイズ最適解ガイド

## 📱 320px〜430px（iPhone SE/12想定）を最優先

### 1. タップ領域（Touch Targets）
- **最小サイズ**: 44px × 44px（Apple HIG推奨）
- **推奨サイズ**: 48px × 48px（Android Material Design推奨）
- **ボタン間隔**: 最小8px、推奨12px
- **重要CTA**: 56px × 56px（画面下部固定ボタン）

### 2. フォントサイズ
- **見出し1（ページタイトル）**: 24px（1.5rem）
- **見出し2（セクションタイトル）**: 20px（1.25rem）
- **見出し3（カードタイトル）**: 16px（1rem）
- **本文**: 14px（0.875rem）
- **キャプション/補足**: 12px（0.75rem）
- **最小サイズ**: 11px（これ以下は避ける）

### 3. パディングとマージン
- **コンテナパディング**: 12px（左右）
- **カードパディング**: 12px（全方向）
- **カード間マージン**: 12px
- **セクション間マージン**: 24px
- **要素間の最小間隔**: 8px

### 4. カードUI
- **カードパディング**: 12px
- **カード角丸**: 12px
- **カード間隔**: 12px
- **カード内要素間隔**: 8px
- **カード最小高さ**: 80px（タップしやすく）

### 5. ボタン
- **高さ**: 44px（最小）〜 48px（推奨）
- **パディング**: 12px 16px（左右）
- **フォントサイズ**: 14px 〜 16px
- **角丸**: 8px 〜 12px
- **ボタン間隔**: 12px

### 6. 入力フィールド
- **高さ**: 44px（最小）〜 48px（推奨）
- **パディング**: 12px（左右）、10px（上下）
- **フォントサイズ**: 16px（iOSでズーム防止）
- **角丸**: 8px
- **ラベルと入力欄の間隔**: 8px

### 7. ナビゲーション
- **ボトムナビ高さ**: 64px（アイコン + 文字）
- **アイコンサイズ**: 24px
- **アイコン下文字**: 10px
- **タブ高さ**: 48px
- **タブフォント**: 14px

### 8. リスト/テーブル
- **行の最小高さ**: 56px
- **行間隔**: 8px
- **アバターサイズ**: 40px 〜 48px
- **アイコンサイズ**: 20px 〜 24px

### 9. モーダル/ダイアログ
- **パディング**: 16px
- **角丸**: 16px（上部のみ）
- **ボタンエリア高さ**: 56px
- **閉じるボタン**: 44px × 44px

### 10. スペーシングシステム（推奨）
```
4px  - 最小単位（細かい調整）
8px  - 小さな間隔（要素間）
12px - 標準間隔（カード内、ボタン間）
16px - 中程度の間隔（セクション内）
24px - 大きな間隔（セクション間）
32px - 最大間隔（ページセクション間）
```

## 🎯 実装例（現在のranking.htmlとの比較）

### 現在の実装
```css
/* カード */
.mobile-card {
    padding: 12px;        ✅ 最適
    margin-bottom: 12px; ✅ 最適
    border-radius: 12px; ✅ 最適
}

/* ボタン */
.tab-btn {
    min-height: 44px;     ✅ 最適
    padding: 12px 16px;   ✅ 最適
}

/* フォント */
.page-title {
    font-size: 1.5rem;    ✅ 最適（24px）
}
```

### 改善提案
```css
/* カード内要素間隔を8pxに統一 */
.mobile-card-stat {
    padding: 8px;        /* 6px → 8px */
    gap: 8px;            /* 追加 */
}

/* フォントサイズの微調整 */
.mobile-card-title {
    font-size: 16px;     /* 現在は継承、明示的に指定 */
    line-height: 1.4;    /* 追加：読みやすさ向上 */
}

.mobile-card-stat-label {
    font-size: 12px;     /* 0.85rem → 12px */
}

.mobile-card-stat-value {
    font-size: 16px;     /* 明示的に指定 */
    font-weight: 600;     /* bold → 600（読みやすさ） */
}

/* 入力フィールド */
.filter-select {
    font-size: 16px;     /* iOSズーム防止 */
    padding: 12px;       /* 上下左右統一 */
}
```

## 📊 画面サイズ別の推奨値

### 320px（iPhone SE）
- コンテナパディング: 12px
- フォントサイズ: 14px（本文）
- カードパディング: 12px
- ボタン高さ: 44px

### 375px（iPhone 12/13）
- コンテナパディング: 16px
- フォントサイズ: 15px（本文）
- カードパディング: 16px
- ボタン高さ: 48px

### 430px（iPhone 14 Pro Max）
- コンテナパディング: 20px
- フォントサイズ: 16px（本文）
- カードパディング: 16px
- ボタン高さ: 48px

## ✅ チェックリスト

- [x] タップ領域が44px以上
- [x] ボタン間隔が12px以上
- [x] フォントサイズが11px以上
- [x] カードパディングが12px以上
- [x] 入力フィールドが44px以上
- [x] 行の高さが56px以上
- [ ] フォントサイズが16px（入力フィールド）
- [ ] 行間（line-height）が1.4以上

## 🚀 次のステップ

1. 入力フィールドのフォントサイズを16pxに統一
2. カード内要素間隔を8pxに統一
3. 行間（line-height）を追加して読みやすさ向上
4. タッチフィードバック（:active）の強化

